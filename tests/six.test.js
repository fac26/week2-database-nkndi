const test = require("node:test");
const assert = require("node:assert");
const { searchProducts } = require("../src/model/products.js");

test("Volume added to app and seeded", () => {
  assert.equal();
});
