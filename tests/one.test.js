const test = require("node:test");
const assert = require("node:assert");
const { searchProducts } = require("../src/model/products.js");

test("User can search data by name of book title", () => {
  assert.equal();
});

test("User can add new book title to database, come back later, and find data is still there", () => {
  assert.equal();
});
