const test = require("node:test");
const assert = require("node:assert");
const { searchProducts } = require("../src/model/products.js");

test("User can search by year of book release", () => {
  assert.equal();
});
